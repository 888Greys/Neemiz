.next
node_modules
out
dist
# TypeScript incremental build cache — regenerated on every build
tsconfig.tsbuildinfo
*.tsbuildinfo
.env*.local
# Real secrets — never commit. Local + Vercel-pulled env files.
.env
.env.vercel.preview
.env.vercel.production
npm-debug.log*

# clerk configuration (can include secrets)
/.clerk/

# Images — hosted on Cloudflare R2, not tracked in git
/public/games/
/public/hero/
/public/leagues/
/public/promo-ball.jpeg
/public/promo-2.avif
.vercel
*.tsbuildinfo
dev-*.log

# signer service
signer/node_modules
signer/data
signer/.env

# stray one-off build snapshot
.next-preadmin-reset-*
.wrangler/
